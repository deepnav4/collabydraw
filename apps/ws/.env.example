# Server Configuration
PORT=8080
NODE_ENV=development

# Database Connection
DATABASE_URL="postgresql://username:password@localhost:5432/collabydraw"

# JWT Secret (must match frontend JWT_SECRET)
JWT_SECRET="your-jwt-secret-here-must-match-frontend"
